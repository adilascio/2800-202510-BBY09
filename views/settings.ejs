<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Settings</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>

<body class="container py-5">
  <h2 class="mb-4">Settings</h2>

  <form action="/settings" method="POST">
    <input type="hidden" name="lat" id="latInput">
    <input type="hidden" name="lng" id="lngInput">
    <div class="form-check form-switch mb-4">
      <input class="form-check-input" type="checkbox" name="shareLocation" id="shareLocationToggle"
             <%= user.shareLocation ? 'checked' : '' %>>
      <label class="form-check-label" for="shareLocationToggle">Enable Location Sharing</label>
    </div>

    <button type="submit" class="btn btn-primary">Save Settings</button>
  </form>

  <a href="/profile" class="btn btn-link mt-3">Back to Profile</a>

  <%- include('templates/navbar', { activeTab: '' }) %>
</body>
</html>
