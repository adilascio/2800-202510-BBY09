<nav class="navbar fixed-bottom bg-white" style="box-shadow: 0 -4px 8px rgba(0,0,0,0.1);">
  <div class="d-flex w-100 justify-content-around align-items-center py-2">

    <!-- Messages Tab -->
    <button
      class="btn py-2 <%= activeTab === 'messages' ? 'text-primary' : '' %>"
      onclick="location.href='/messages'"
      aria-label="Messages"
    >
      <img src="/svgs/<%= activeTab === 'messages' ? 'chat-left-text-fill' : 'chat-left-text' %>.svg"
           width="24" height="24"
           alt="Messages">
    </button>

    <!-- Learning Tab -->
    <button
      class="btn py-2 <%= activeTab === 'learning' ? 'text-primary' : '' %>"
      onclick="location.href='/tutor'"
      aria-label="Learning"
    >
      <img src="/svgs/<%= activeTab === 'learning' ? 'mortarboard-fill' : 'mortarboard' %>.svg"
           width="32" height="29"
           alt="Learning">
    </button>

    <!-- Puzzles Tab -->
    <button
      class="btn py-2 <%= activeTab === 'puzzles' ? 'text-primary' : '' %>"
      onclick="location.href='/game'"
      aria-label="Puzzles"
    >
      <img src="/svgs/<%= activeTab === 'puzzles' ? 'puzzle-fill' : 'puzzle' %>.svg"
           width="28" height="28"
           alt="Puzzles">
    </button>

    <!-- Home Tab -->
    <button
      class="btn py-2 <%= activeTab === 'home' ? 'text-primary' : '' %>"
      onclick="location.href='/home'"
      aria-label="Home"
    >
      <img src="/svgs/<%= activeTab === 'home' ? 'house-fill' : 'house' %>.svg"
           width="27" height="27"
           alt="Home">
    </button>

  </div>
</nav>
